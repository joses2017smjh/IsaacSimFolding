# The dependency-light half: src/lehome_fold and tests/test_pure.py.
# Verified by import audit -- these two are the only third-party names either
# imports. 37 tests run anywhere both exist, with no GPU and no simulator.
numpy>=1.24
torch>=2.0

# The simulator half needs Isaac Sim 5.1, Isaac Lab (forked), OpenUSD/Hydra
# Storm and LeRobot 0.4.3 inside an Apptainer image, plus the challenge asset
# pack and a Slurm cluster. It is NOT pip-installable and is deliberately not
# listed here -- see slurm/_env.sh for how that environment is assembled.
