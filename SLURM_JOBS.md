# Job ledger

Every job submitted from this repo, updated at submission. Kept because a
deliverable went missing once when it was not.

| job | name | date | state | what it answered |
|---|---|---|---|---|
| 21067439 | `lh-rtx51` | 2026-08-27 | **SEGFAULT (139)** | **G0-a: RTX on Isaac Sim 5.1. It does not render on this cluster.** A40 / driver 595.71.05, `cn-r-2`, died in `createHydraEngine` -> `librtx.scenedb.plugin.so`. Verdict in `results/rtx51_probe_borrowed.txt`. |
| 21067529 | `lh-image` | 2026-08-27 | FAILED | Download of the official image died at 5.7/24.8 GB, `curl: (18)`. `--retry` does not cover exit 18. |
| 21067642 | `lh-image` | 2026-08-27 | FAILED | Retry loop pulled 25 GB but the file was not a valid gzip: the CDN answered the resume with 200 not 206, curl threw away 12 GB client-side and spliced the rest onto the partial. Size matched, content did not. |
| 21067643 | `lh-trainenv` | 2026-08-27 | FAILED | Wanted `lehome.sif`, which is not built. The train route needs no Isaac Sim, so it now reuses `bhl.sif`. |
| 21078863 | `lh-trainenv` | 2026-08-28 | **COMPLETED** | torch 2.7.0+cu128, lerobot 0.4.3, transformers 4.57.6, CUDA available, 7.6 GB. **Stages 1-2 are unblocked.** |
| 21078864 | `lh-image` | 2026-08-28 | FAILED | Download was fine this time (26,676,771,349 bytes, `gzip -t` clean). `apptainer build` still refused it: **the tarball is an OCI layout (`blobs/sha256/...`), not a `docker save` archive**, and apptainer reports that mismatch as "gzip: invalid header" — which sends you hunting a corrupt download that isn't corrupt. Needs `oci-archive://`. |
| 21079091 | `lh-image` | 2026-08-28 | pending | Same 26 GB file, `oci-archive://`. No re-download. |
| 21079092 | `lh-data` | 2026-08-28 | pending | Retries with backoff; picks up from the 946 MB + 8.2 GB already on disk. |
| ~~21078864~~ | | | | *(superseded)* | Same pull via `hf` instead of curl, plus a `gzip -t` check before the 25 GB conversion. Tests the one remaining hope that the official image renders where the pip stack does not. |
| 21078925 | `lh-data` | 2026-08-28 | FAILED | Got the assets (946 MB, complete) and 8.2 of 18.9 GB of demonstrations, then **HTTP 429: the Hub rate-limits by IP and this cluster shares one**. A `HF_TOKEN` lifts the anonymous limit and is the real fix; the job now backs off and resumes without one. |

| 21079138 | `lh-render` | 2026-08-28 | queued | **The Isaac Sim render.** Real SO-101 USD at both bimanual poses, a real Release garment with its fabric texture, the challenge's own overhead camera, arms driven by episode 0 of the released demos — on **6.0**, which renders here. |
| 21079173 | `lh-port` | 2026-08-28 | queued | **How big is the 6.0 port?** All 15 isaaclab symbols the task imports resolve on 3.0.0b2, and the particle cloth is raw PhysX/USD, so it is version-independent. This stands the env up and reports the first real failure. |

| 21083423 | `lh-storm51` | 2026-08-28 | queued | **Is the pincer actually a pincer?** What dies on 5.1 is the RTX *delegate*, not 5.1. Physics on 5.1 is fine and the wheels also ship OpenUSD's Storm rasteriser. If Storm draws, the deviation becomes "different renderer" instead of "different physics" — and the geometric success checker is untouched. |

| 21083432-511 | `lh-storm51` | 2026-08-28 | **RESOLVED** | **5.1 CAN render.** Storm draws without the RTX delegate. Five probes: plugin path, `Parameters.rendererPluginId`, EGL context, compatibility profile, `FrameRecorder` readback. |
| 21083532-582 | `lh-stormscene` | 2026-08-28 | **COMPLETED** | The real challenge scene — robots, textured garment, table — rendered on 5.1 via Storm. Caveat: Storm cannot read MDL, so the robots render flat white. See `docs/STORM.md`. |

| 21090714-916 | `lh-cloth51` | 2026-08-29 | diagnostic chain | Standing the cloth env up on 5.1. Six distinct blockers: camera CLASS stub, zero-image stub, `open3d`, `_get_initial_info()` ordering, `pynput`, and a repeat of the Kit-swallows-`sys.exit` false success. |
| 21090922 | `lh-cloth51` | 2026-08-29 | **CLOTH SIMULATES** | `sim.device=cuda:0`. 0.330 m displacement, garment top fell 0.178 m, official checker executed. PhysX particle cloth needs GPU dynamics; `--device cpu` is policy inference. |
| 21090939-941 | `lh-fold` | 2026-08-29 | **COMPLETED** | 183 frames of the simulated cloth rendered through Storm → `docs/img/cloth_fold.gif`. 20.7% of pixels change first-vs-middle, measured. |
| 21091167 | `lh-cloth51` | 2026-08-29 | COMPLETED | Replay `action` not `observation.state` — 0.225 m, checker `False`. |
| 21091179 | `lh-cloth51` | 2026-08-29 | COMPLETED | Actions + garment pinned to its recorded `object_initial_pose` — 0.325 m, checker `False`. Open-loop replay does not reproduce the fold; see `docs/CLOTH.md`. |

| 21093704 | `lh-cloth51` | 2026-08-29 | COMPLETED | Re-ran the sim recording **robot link poses** (365x7x3 per arm) so the animation can show the arms, not just a garment moving itself. |
| 21093740 | `lh-fold` | 2026-08-29 | FAILED | Link names were written as a pickled object array and would not read back. Fixed on the read side with the known body order rather than re-running a 45 s simulator boot for a constant. |
| 21093748 | `lh-fold` | 2026-08-29 | COMPLETED | **Robot + cloth animation.** 14 link transforms across two SO-101s. Each link references `/visuals/<link>` explicitly — the USD's meshes are a *sibling* of its defaultPrim, which is why the arms had been rendering flat white. |
| 21093756 | `lh-fold` | 2026-08-29 | COMPLETED | Reframed on the whole workspace; a camera fitted to the cloth bounds was clipping an arm out of shot. → `docs/img/robot_fold.gif` |
| 21093764 | `lh-sweep` | 2026-08-29 | COMPLETED | **Four garments, four checker-verified failures.** Cloth moved 0.22–0.33 m each. Open-loop replay does not fold; see `docs/CLOTH.md`. |
| 21093929/30 | `lh-bc` | 2026-08-29 | FAILED | First Stage 1 attempt. Two bugs: `lerobot.scripts.train` does not exist in 0.4.3 (it is `lerobot_train`), and **torchcodec cannot load** — `libnppicc.so.12` missing, no CUDA NPP runtime. RGB is video-encoded so a decoder is not optional. |
| 21093962/63 | `lh-bc` | 2026-08-29 | CANCELLED | Relaunched with `video_backend: pyav`, then cancelled: two 24 h requests reserve 2,880 GPU-min and parked both behind `MaxGRESRunMinsPerAccount` — which was also blocking the user's own `v2-train` array. |
| 21093972 | `lh-bc` | 2026-08-29 | CANCELLED | Cancelled at 17 min and relaunched as 21094090. I had this logged as RUNNING and credited it with the training results; it produced no checkpoints. |
| 21094090 | `lh-bc` | 2026-08-30 | TIMEOUT | **Stage 1 BC, SmolVLA — the run that actually trained.** Ran the full 6 h and was killed by the wall clock at step 15K, epoch 1.85 of a planned 30K (half the schedule, per the checkpoint's own train_config.json). 265,798 frames / 1,000 episodes, 450M total / 100M trainable, batch 32. Loss 1.505 (step 100) -> 0.065 (step 15K). Checkpoints 005000 / 010000 / 015000, `last -> 015000`. TIMEOUT is the expected stop here, not a failure: checkpointing every 5k means the run is resumable and 015000 is complete. Numbers are from the run's full stdout log — with wandb disabled LeRobot writes no event file, so there is no independent series to cross-check against. |
| 21094000 | `lh-sik` | 2026-08-29 | **COMPLETED** | **Storm renders inside a live Kit process.** `GL 4.6.0 alongside Kit`, `Record() -> True`. So the observation pipeline is feasible in-process — a trained policy can actually be rolled out here. |
| 21100435 | `lh-roll` | 2026-08-30 | FAILED | First rollout attempt. Died in `env.reset()`: `GarmentObject` had no `initial_points_positions`, because `_get_initial_info()` reads `_cloth_prim_view.get_world_positions()` on GPU and the view was never bound to the physics sim view — `cloth_sim51.py` binds it, this script did not. A bare `except Exception: pass` around the setup call hid the real failure until it resurfaced 40 lines later. It also printed `VERDICT success=False` over the traceback, which would have mislabelled a crash as a policy failure in the GIF set. Both fixed. |
| 21100458 | `lh-roll` | 2026-08-30 | FAILED | Setup fix worked — `cloth_prim_view.initialize(sim_view) ok`, `_get_initial_info() ok`, cloth spawned, Storm stage built. Died later loading the policy: `make_policy(ds_meta=...)` wanted the training set at `$LEHOME/Datasets/...`, which is not where it lives (`lehome-data/Datasets/`), and LeRobot answered the missing directory by fetching repo `lehome` from the Hub → 401. The dependency was pointless: the feature spec is in the checkpoint's `config.json` and the normaliser ships beside it. |
| 21100470 | `lh-roll` | 2026-08-30 | FAILED | Policy loaded offline (450M). Crashed on the first inference: `resize_with_pad` got `[256, 640, 3]`. Two distinct mismatches. (1) Format — Storm returns HWC uint8, SmolVLA wants batched `(b,c,h,w)` float. (2) Size — the observer asks for 640x480 but `FrameRecorder` takes only a width and derives height from the camera's aperture ratio; `_camera()` never set `VerticalAperture`, so USD kept its 15.2908 default and rendered 640x256 against 640x480 training frames. The guarded verdict held: logged `NO VERDICT ... (rendered=1)` rather than a fake `success=False`. |
| 21100489 | `lh-roll` | 2026-08-30 | **COMPLETED** | **First policy-driven episode in this project — the full loop closes.** env -> Storm observation -> SmolVLA -> action -> env -> official checker, 300 steps, all 300 rendered at a verified 640x480. Verdict: `success=False`, and LeHome's own log agrees (`[Success Check] Final result: Failed ✗`). Not a truncation artifact — the mean demonstration is 265.8 steps, so the policy got slightly longer than a typical demo. Produced no GIF: `frames` was declared and never appended to, and the observer overwrites its PNGs each step. Fixed after the fact. |
| 21100500 | `lh-sweep` | 2026-08-30 | FAILED | All 8 array tasks died in 3 s, exit 127. The wrapper chained to the rollout script via `$(dirname "$0")`, but Slurm executes a COPY out of `/var/spool/slurmd/job<id>/`, so `$0` never points into the repo. Absolute path now. |
| 21100518 | `lh-sweep` | 2026-08-30 | **COMPLETED (8/8, all `success=False`)** | Sweep over 8 garments. Tasks 0-3 (`Top_Short_Seen_0/1`, `Top_Long_Seen_0/1`) all returned `success=False` with GIFs and JSON written, then **hung in Kit shutdown** — task 0 idled 39 min holding a GPU against the account's GPU-minute cap. Cancelled once artefacts were confirmed on disk; 4-7 started immediately and carry the new `DIAG` instrumentation. Root cause fixed in the script: `app.close()` hangs, and a hang is not an exception, so it is now skipped in favour of `os._exit` after flushing. Diagnostic finding across these episodes: `dist(p0,p4)` moved 28.86 -> 29.04 against a 9.45 threshold — the garment is essentially untouched, not narrowly mis-folded. |
| 21100772 | `lh-reach` | 2026-08-30 | **COMPLETED** | Reach diagnostic, 120 steps. Refuted my "robot and garment are not co-located" reading: they are ~11 cm apart, and the cloth centroid `(0.003, 0.025, 0.529)` sits between arm bases at `(±0.230, -0.250, 0.500)`. Cloth z fell 0.663 -> 0.529 in the first 25 steps, which is the gravity settle that produced the early `dcloth` bump. Min link-to-cloth: 11.72 -> 10.82 -> 10.60 -> 10.58 -> 10.57 -> 10.55 cm — monotonic and asymptotic while the arms sweep 1.5 rad, which is the signature of a STATIC proximal link setting the minimum, so the metric says nothing about the gripper. Also first job to exit cleanly (`=== done ===`) under the `os._exit` fix. Units resolved: the checker works in centimetres. |
| 21100791 | `lh-links` | 2026-08-30 | **COMPLETED** | Per-link breakdown. The all-link minimum was indeed pinned by proximal links (index 0-2 of 7); the END EFFECTORS start 32.8 cm (left) / 27.2 cm (right) from the cloth, close once to ~13 cm, then oscillate in a 12.5-14.7 cm band for the rest of the episode and never contact it. So the grippers approach and then hover, they do not stall far away and they do not touch. |
| 21100802/08 | `lh-settle` | 2026-08-30 | FAILED / **COMPLETED** | 802 died on my own `NameError` (`cc` used one line above its definition; the verdict guard correctly logged `NO VERDICT` instead of a fake failure). 808 ran clean and **refuted the settle hypothesis**: with the cloth fully settled first (`cloth_z=0.5294`) the policy still drove the end effectors to z~0.65 and hovered, `success=False`. So the 13.4 cm settle drop matching the ~13 cm hover gap was a coincidence, not a cause. What the run did establish: `ee_z` sits at 0.628-0.675 all episode while the cloth rests at 0.529 — a systematic ~12 cm vertical offset between where the policy reaches and where the cloth is. |
| 21100829 | `lh-replay` | 2026-08-30 | **COMPLETED** | **Overturned my "the scene is broken" reading.** Replaying demo episode 0's 364 actions FOLDED the cloth: `dist(p2,p3)` 41.65 -> **3.27** cm against a <=12.15 threshold, 3/5 conditions passing, grippers reaching 5.0 cm. So cloth physics, contact and grasping all work — the env is sound. Still `success=False`, because the actions were replayed against a DIFFERENT spawn pose than they were recorded at (env sampled `[-0.016, 0.018, 0.67]`; episode 0 was recorded at `[-0.043, -0.016, 0.67]`, ori `[-2.4, -16.6, 0]`), so an open-loop trajectory partially misses. Comparison that matters: the POLICY passes only the 2 trivial `>=` conditions, never closes below 12.5 cm, and moves the cloth 0.18 cm total — far worse than a mispose'd replay. |
| 21100868/875/985/998 | `lh-match` | 2026-08-30 | FAILED | Four failed attempts to pin the garment pose, none reaching physics. (1) `sbatch --export` splits ITS OWN argument on commas, so a comma-separated pose arrived truncated to one number. (2) argparse read the leading `-` of the negative x as an option flag; fixed with `--opt=value`. (3) Editing `objects.common` in the YAML moved only z, because `_get_config_value` prefers the per-garment `garment_config`. (4) Overriding `_get_initial_pose` pinned the SPAWN but `env.reset()` re-samples from `soft_reset_*` ranges. Each was caught by checking the `Reset complete` line rather than trusting the pin. |
| 21101004 | `lh-match` | 2026-08-30 | **COMPLETED** | **Pose-matched replay: 4/5 conditions.** Pinned via `_get_config_value`, the one seam all four ranges resolve through; `Reset complete` confirmed `pos: [-0.04258476, -0.01609312, 0.67], ori: [-2.3939, -16.63032, 0.0]`, scale 0.45. Two real folds: `dist(p0,p4)` 28.32 -> **3.87** (<=9.45 ✓), `dist(p2,p3)` 37.96 -> **4.44** (<=12.15 ✓). Only `dist(p1,p5)` misses at 15.76 vs <=9.0. Matching the pose moved replay from 3/5 to 4/5. Also exposed a naming collision: this GIF OVERWROTE the policy's `rollout_Top_Short_Seen_0_failure.gif`, since the name encoded garment+verdict but not what produced the episode. Filenames now carry mode and episode; the overwritten file was renamed to what it actually holds. |
| 21101021 | `lh-mrep` | 2026-08-30 | FAILED | Concurrency bug of mine: `FRAMES` was derived from `$GARMENT` alone, but episodes 1-4 are all `Top_Short_Seen_0`, so four array tasks shared one Storm workdir and clobbered each other's fixed-name renders mid-flight — `UnidentifiedImageError` on a half-written PNG, a missing `obs_stage.usda`, crashes at 26/219/0 frames. The earlier sweep only survived because every task had a distinct garment. Workdir is now per-run via `RESULT_SUFFIX`. |
| 21101040 | `lh-mrep` | 2026-08-30 | **COMPLETED — 6/8 SUCCESS** | **First verified folds in the project.** Matched-pose replays of episodes 1-4 and 25-28: ep2, ep4, ep25, ep26, ep27, ep28 all returned `success=True` with 5/5 conditions and `[Success Check] Final result: Success ✓`; ep1 and ep3 hit 4/5 and 3/5 (open-loop drift, which differs per episode). ep2 detail: `dist(p0,p4)=5.06<=9.45`, `dist(p2,p3)=5.65<=12.15`, `dist(p1,p5)=5.75<=9.0`. All eight finished in ~2 min each under the `os._exit` fix — no shutdown hangs. **Proves env, cloth physics, contact, observation pipeline and scoring are all correct end to end**, and isolates the failure to the POLICY: same pipeline and checker, replay 6/9 vs policy 0/8. |
| 21105202 | `lh-pol0` | 2026-08-30 | **COMPLETED** | Restored the `Top_Short_Seen_0` POLICY rollout whose GIF the pose-matched replay had overwritten. `success=False`, 300 frames — completing a SAME-GARMENT policy-vs-replay pair, the strongest form of the comparison. |
| 21105367 + 21105368 | `lh-wrist` / `lh-wpol` | 2026-08-30 | **COMPLETED** | **Wrist-camera bug, found from a user observation that the GIFs' side panels looked static.** Measured it: left/right panels had 14% of pixels changing but **0.00% changing by more than 60** (shading drift, no object motion) vs 0.37% for the top panel. The challenge mounts wrist cameras ON the grippers; `storm_obs.py` pinned them to FIXED world positions aimed ~0.37 m from the garment, so both rendered empty table — **the policy was fed two dead frames out of three.** Parenting them under the gripper Xform made them move (13.1% / 17.4% of pixels changing >60) but they rendered FLAT BACKGROUND, because the challenge declares the offsets with `convention="ros"` (forward +Z, up -Y) while a USD camera looks down -Z. Replay control still `True` (open-loop, cameras cannot affect it); policy still `False`. |
| 21105419 | `lh-wrist2` | 2026-08-30 | **COMPLETED** | ROS->USD camera conversion (extra 180 deg about X). Wrist views now show the gripper jaws with the garment directly in front of them — a real wrist view. `success=False` is expected and not a fold result: only 120 steps ran, and this fold completes near step 344. |
| 21105376 | `lh-types` | 2026-08-30 | **COMPLETED — 9/12, all four classes** | Matched-pose replays across every garment class. Top_Short 2/2, Top_Long 3/4, Pant_Short 2/3, Pant_Long 2/3. Each class has its own thresholds (short top `[9.45, 12.15, 9.0, 13.05, 8.55]`, long top `[11.7, 10.8, 10.8, 9.9, 9.0]`), so this is the first evidence the pipeline folds anything beyond short tops. NOTE: I reported ep250/251 as "both failed" from a still-running log; ep251 in fact succeeded — verdicts must be read from finished runs. |
| 21105496/97 | `lh-wp2` | 2026-08-30 | **COMPLETED — hypothesis refuted** | Policy re-run with all three cameras genuinely showing the scene. Both `success=False`, 2/5 conditions, on `Top_Short_Seen_1` and `Top_Long_Seen_0`. Behaviour is essentially unchanged from the broken-camera run: `ee_z` 0.640/0.635 vs 0.663/0.678, gripper gap 11-15 cm vs 13-16 cm, `dcloth_max` 0.0168 vs 0.0144 m. **So the wrist-camera bug, though real, was NOT the cause of the policy failure** — and my earlier domain-gap claim was equally premature. The informative part is the insensitivity itself: a policy whose output barely moves when two of its three visual inputs go from blank to correct is not using vision. That is consistent with a BC checkpoint stopped at 15K of 30K having learned a mean trajectory prior rather than visual servoing. |
| 21105376 | `lh-types` | 2026-08-30 | **RUNNING** | Array 0-11%6 of matched-pose replays across **all four garment classes** — Top_Short (eps 5, 6, 275), Top_Long (250-252), Pant_Short (500-502), Pant_Long (750-752). Every verified fold so far was a Top_Short, and the four classes have different fold criteria, so a result on one says little about the others. First sweep with gripper-mounted wrist cameras. |
| 21124825 | `lh-bcres` | 2026-09-01 | **RUNNING** | **Task 1 — resume Stage 1 BC toward the full 30K.** The original run died on a wall clock at step 15K, which is HALF the schedule, not the "20K" I had published (the checkpoint's own `train_config.json` says `steps: 30000`; README and two ledger rows corrected). `--resume=true` restores optimizer and scheduler state from `training_state/`, and the log confirms it picked up at step 15K with loss 0.067 against the 0.065 it stopped at. This is the falsifiable test of the surviving hypothesis: two earlier explanations (domain gap, wrist cameras) died, and a policy that indifferent to its vision looks undertrained. 5.5 h not 6 — `MaxGRESRunMinsPerUser` is already saturated by the user's own jobs and the cap counts reserved minutes. |
| 21124843 | `lh-fid` | 2026-09-01 | FAILED | `/usr/bin/bash: cannot execute binary file`. `lh_exec` takes a SCRIPT PATH and runs it with bash, so `lh_exec bash -c "..."` became `bash bash -c ...`. The repo already has an `inner/` convention for exactly this; I bypassed it. |
| 21125293 | `lh-fid` | 2026-09-01 | FAILED | `CKPT: unbound variable`. `lh_exec` runs apptainer with `--cleanenv` and forwards ONLY the names in `LH_FORWARD_VARS`, so `export` alone does not carry a variable into the container. The rollout jobs never hit this because they call apptainer directly rather than through `lh_exec`. |
| 21125322 | `lh-fid` | 2026-09-01 | FAILED | `Could not load libtorchcodec`. `action_fidelity.py` built `LeRobotDataset` without a `video_backend` and used `episodes=` — both already solved in `train_value.py`, both repeated in this newer script. Fixed together rather than one per failed job: `pyav` pinned, and the episode subset built by filtering `episode_index`. |
| 21125336 | `lh-fid` | 2026-09-01 | **RUNNING** | Task 2 retry with pyav and index-based episode filtering. |
| 21124913 | `lh-value` | 2026-09-01 | FAILED | `AttributeError: 'int' object has no attribute 'length'` — a half-applied refactor of mine. I changed `load_outcomes` to return a dict keyed by `episode_index` and updated `build_targets`, but missed the class-balance line, where `for o in outcomes` iterates a dict's KEYS and handed an int to `success_targets`. Fixed by normalising to a single dict representation everywhere rather than branching on type per call site. |
| 21124976 | `lh-value` | 2026-09-01 | FAILED | `RuntimeError` inside a DataLoader worker: `train_value.py` built `LeRobotDataset` without a `video_backend`, so it defaulted to torchcodec, which cannot load on this cluster (`libnppicc.so.12`). The BC config has pinned `pyav` since the first training run; this script had never been given the same treatment because it had never been run against the video dataset before. Failure surfaces in a worker rather than at construction, so it looks like a data problem rather than a config one. |
| 21125000 | `lh-value` | 2026-09-01 | FAILED | `KeyError: 'observation.language.tokens'`. `train_value.py` called `policy.forward` on a raw dataset batch; SmolVLA reads tokenised language that only the POLICY PROCESSOR creates. The script had no processor at all — it predates the rollout work, where this was already solved. |
| 21125173 | `lh-value` | 2026-09-01 | CANCELLED | Reached the subset (`6066 frames from 20 scored episodes`) then hit `RuntimeError: size of tensor a (250) must match b (264)` in `make_att_2d_masks`. SmolVLA's `forward` is its TRAINING forward and builds an attention mask from the action-suffix length, so the dataset's single `(B, D)` action is the wrong shape. Cancelled early — the 6066 figure also exposed a labelling bug of mine, below. |
| 21125257 | `lh-value` | 2026-09-01 | FAILED | Got furthest yet — subset built, `success targets: n=6066 positive=0.758` (real class balance from the corrected lengths) — then `RuntimeError: Expected all tensors to be on the same device` at `wrap()`. The value heads are built LAZILY on the first captured feature, so the module's earlier `.to(device)` could not have reached them: they did not exist yet. Built on the CPU default, then called with a CUDA feature. |
| 21125275 | `lh-value` | 2026-09-01 | **RUNNING** | Stage 2, 5th retry. `_build_heads` now takes the feature it will be driven by and constructs on its device — fixed in `policy_wrap.py` rather than patched at the call site, so any lazy build lands correctly. Device only, not dtype: a bf16 backbone should not drag the heads (the thing actually being optimised) down with it, so the feature is cast to the heads' dtype at the call site instead. 34 tests pass. |
| 21124942 | `lh-pi05` | 2026-09-01 | CANCELLED | Sat PENDING on `QOSMaxMemoryPerUser` and would never have started: 128G against a per-USER total already carrying the user's 2x v2-train, 2x grip-train and ice-ppo, plus this project's own 96G training and value jobs. A request that cannot schedule is not conservative, it is invisible. |
| 21125323 | `lh-pi05` | 2026-09-01 | **RUNNING** | Task 4 resubmitted at 80G so it can actually schedule. |

## What the ledger says about the project

The renderer is the root of everything still blocked. `21067439` settled that
Isaac Sim 5.1 cannot render here and that it is a driver-version problem, not
hardware and not node selection — every GPU driver on this cluster is 595 or
610, and the 6.0 probe that worked ran on the same node class.

`21078863` is the one that matters for making progress anyway: Stage 1 BC and
Stage 2 value-head training read the released LeRobot dataset and never open a
simulator, so they run today on `LH_ROUTE=train`.

**The evaluation path is now open.** `21094000` showed Storm rendering inside a
live Kit process, which is the missing piece: the env feeds the policy three RGB
images, the RTX cameras segfault on 5.1, and the current stub returns zeros — a
VLA looking at black frames cannot fold anything. In-process Storm means
official physics, the official geometric scorer, and only the images deviating.

**Two conclusions in this ledger were overturned by later jobs**, and both are
worth reading in order rather than trusting the earliest confident statement:
5.1 renders (via Storm), and 5.1 simulates cloth (on GPU physics). The
"no stack can do both" claim was wrong on both halves.

**The renderer conclusion has been overturned.** `21083511` established that
5.1 renders through OpenUSD's Storm rasteriser — what segfaults is the RTX
delegate, not 5.1. Since 5.1 is the stack that still has PhysX particle cloth,
the project has a path that keeps official physics AND the official geometric
scorer, deviating only in the images. The remaining question is the size of
that domain gap, and it is measurable.

**Open ask — one command, needs your HF account.** The Hub rate-limits
anonymous traffic by IP and every job on this cluster shares one address. A
read-scoped token lifts it. I cannot create one for you; the jobs are already
wired to pick it up from a file:

```bash
printf %s 'hf_xxxxxxxxxxxx' > /nfs/hpc/share/$USER/Humanoid_Lite/.hf_token
chmod 600 /nfs/hpc/share/$USER/Humanoid_Lite/.hf_token
```

Token at <https://huggingface.co/settings/tokens>, read scope. `slurm/_env.sh`
reads that file into `HF_TOKEN` and forwards it into the container. It is a
FILE rather than a job-script variable so the credential never lands in git, in
a job log, or in `scontrol show job` output — and `.hf_token` is gitignored.
Nothing fails without it; the pull just falls back to anonymous with backoff.
